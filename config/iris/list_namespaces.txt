write $system.Version.GetISCProduct(), !
write $namespace, !
write ##class(%Dictionary.ClassDefinition).%ExistsId("%SYSTEM.Context.HealthShare.Manager"), !
write ##class(%Dictionary.ClassDefinition).%ExistsId("HS.Util.Installer.Foundation"), !
do ##class(%SYS.Namespace).ListAll(.nsp)
set ns = ""
for {
  set ns = $order(nsp(ns))
  if ns = "" quit
  write ns, !
}
halt
