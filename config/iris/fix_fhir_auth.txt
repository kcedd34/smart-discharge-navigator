set app = ##class(Security.Applications).%OpenId("/fhir/r4")
write "App found: ",$isobject(app),!
write "Name: ",app.Name,!
write "AutheEnabled: ",app.AutheEnabled,!
write "MatchRoles: ",app.MatchRoles,!
set app.AutheEnabled = 64
set app.AutheEnabled = app.AutheEnabled + 32
do app.%Save()
write "Updated AutheEnabled to: ",app.AutheEnabled,!
halt
